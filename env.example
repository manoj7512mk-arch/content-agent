# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# Apify (Step 2)
APIFY_API_TOKEN=

# Gemini (Step 3) - free tier, no card required, from aistudio.google.com
GEMINI_API_KEY=
